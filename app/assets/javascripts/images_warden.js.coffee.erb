window.avatarImgError = (image) ->
  image.onerror = ""
  image.src = "<%= asset_path('user.png') %>"
  true

window.podcastArtImgError = (image) ->
  image.onerror = ""
  image.src = "<%= asset_path('microphone.png') %>"
  true

$(document).ready ->
  # fix(stub) broken images
  $('.avatar').error (e) ->
    avatarImgError(this)
  $('.podcast-art').error (e) ->
    podcastArtImgError(this)
